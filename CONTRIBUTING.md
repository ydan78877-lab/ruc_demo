# 协作开发规范

## 日常流程

1. 同步 `develop`，然后创建 `feature/...` 或 `fix/...` 分支。
2. 一个分支只处理一个相对独立的功能或问题。
3. 提交前运行必要的类型检查、数据校验和目标端构建。
4. 提交 Pull Request 到 `develop`，说明改动、验证结果、数据影响和界面截图。
5. 交叉检查通过后合并，不直接强制推送 `develop` 或 `main`。

## 必须审核的改动

- CloudBase 集合、存储路径、云函数授权和管理员鉴权。
- 用户资料、OpenID、班级/课程成员和个人事项的隔离逻辑。
- 数据库结构、迁移脚本与删除或覆盖数据的操作。
- 微信体验版/正式版、管理后台和任何公网部署。

## 环境规则

- 日常开发只使用开发 CloudBase 环境和模拟账号。
- `main` 对应的内测正式环境只由项目所有者访问和发布。
- 不从正式环境导出用户数据到本地、开发环境、Pull Request 或 GitHub Issue。
- 不在代码、截图、日志或提交记录中写入密码、Token、私钥和真实用户信息。

## 发布流程

1. 从 `develop` 创建发布 Pull Request 到 `main`。
2. 确认网页、管理后台、小程序构建和关键真机页面验证通过。
3. 记录版本号和本次变更。
4. 由项目所有者使用自己的微信开发者工具和正式凭据上传。
